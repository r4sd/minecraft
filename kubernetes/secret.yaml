# Minecraft サーバー機密情報
# 本番環境では Sealed Secrets または External Secrets の使用を推奨
apiVersion: v1
kind: Secret
metadata:
  name: minecraft-secrets
  namespace: minecraft
type: Opaque
stringData:
  # RCON パスワード（必ず変更すること）
  RCON_PASSWORD: "change-me-in-production"
